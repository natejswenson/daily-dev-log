---
title: "The grading rules stopped being mine and started being yours (v0.9.0)"
date: 2026-06-23
project: local-fitness
version: v0.9.0
summary: "Five hardcoded coaching choices (whether walks count, how lenient the grade bands are, how far back to look for a projected finish) are now user settings, each defaulting to the old hardcoded value so a fresh clone is unchanged."
---

## What Shipped

This app is public, but a handful of coaching decisions were baked into the code as my preferences. Whether a recovery walk counts toward an easy day. How close to the target distance counts as done versus partial. How far back to look for a best effort when projecting a race finish. Reasonable defaults, but they were mine, and anyone else cloning the repo was stuck with them.

So I pulled five of those choices out of the code and made them settings, each defaulting to exactly what was hardcoded before, so a fresh clone behaves the same and nobody has to configure anything to start.

The app already had three places config could live: environment variables for deployment and secrets, a small key-value settings table for per-user values like the step goal, and a notes file for the coach's conversational memory. I did not want a fourth. So these knobs resolve in a clear order: a value in the settings table wins, then an environment variable, then the built-in default. You can set one live with a CLI command and it takes effect immediately, or write it once in your env file, or do nothing and get the sensible default.

The grading functions are pure, with no database access, which is what keeps them easy to test. The constraint was getting user settings into them without breaking that, so the callers resolve the config once and pass it in. The adversarial review caught the parts I would have shipped wrong: an empty setting value silently flipping a true/false knob to false instead of the default, no guard against a fraction pair that inverts the grade bands, and a negative lookback window that quietly erases the projected finish. Each of those is now validated, with anything invalid falling back to the default. The defaults reproduce the old behavior exactly, which is the one property worth pinning with a test: change a default by accident and the suite fails.

## What's Next

The same pattern fits the knobs I left hardcoded for now, like the baseline window and the fatigue and fitness time constants; those are the next candidates once there's a reason to tune them.

## Commits

- feat: make grading + projection behavior user-configurable (0.9.0) ([8047c4e](https://github.com/natejswenson/local-fitness-dude/commit/8047c4e237028edc6ad6e0dd352cae3d64130105))
- docs: revise configurable-grading design per quality-gate (2 rounds + look-harder, 3->0) ([adad68f](https://github.com/natejswenson/local-fitness-dude/commit/adad68fffec421d17c5609af04a12f368d3c9d95))
- docs: design for user-configurable fitness behavior (grading knobs) ([f28e479](https://github.com/natejswenson/local-fitness-dude/commit/f28e479101ef10122b814c6f2115a11e123cd09d))
- Merge pull request #33 from natejswenson/feat/configurable-grading ([2f647a1](https://github.com/natejswenson/local-fitness-dude/commit/2f647a16936b1a9433113b00fcfd75932cc27fe0))
